# Analytical Mechanics Notes

This folder contains note pages and PDF placeholders for Analytical Mechanics.

Add PDF files inside `pdf/` and update the matching object in `assets/js/content.js` by setting `pdfAvailable: true`.
